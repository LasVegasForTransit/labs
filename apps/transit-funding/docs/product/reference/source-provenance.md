# Source provenance

TransitFunding entered Labs from precursor commit `8d804d77a85d03d7745a1268f19cbd111955fb1b`. The
precursor had no configured remote, so the commit hash is the durable import identity.

## Import guarantees

The Labs history records the import commit and keeps the precursor commit in project metadata. The
import preserves the fiscal model, tests, documentation, sources, and license records while
replacing obsolete host and deployment assumptions with the Labs project contract.

The precursor checkout remains an archival source and never acts as a second deployment owner.
